# Camera 3 — Fragrance & Promo Aisle
Drop the recorded `.mp4` (or `.mov`) file here. Filename must start with
`YYYYMMDDHHMMSSmmm` (e.g. `20260608003129784.mp4`) so the pipeline can
parse the real recording-start time. Files are gitignored (too large for
GitHub), so each contributor brings their own footage.
